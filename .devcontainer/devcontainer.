📦 Development Container SetupThis project includes a Dev Container configuration designed to provide a stable, reproducible environment for NetworkBuster agents, ledger‑bound workflows, and cross‑language tooling. The container ensures that every contributor—and every automation agent—runs the project with the same versions of Node, Python, Git, and essential VS Code extensions.🔧 What’s Inside the Dev ContainerThe devcontainer.json uses the universal base image:Copymcr.microsoft.com/devcontainers/universal:2On top of this, the environment includes:Common utilities for shell, debugging, and system toolsGit for agent‑README automationNode (LTS) for workflow scripts and agent logicPython 3.11 for ledger engines, mapping tools, and analysis pipelinesVS Code extensions for formatting, linting, GitHub Actions, and Copilot assistanceThese components ensure that NetworkBuster agents behave consistently across machines and CI environments.🚀 How to Use the Dev ContainerOpen the repository in VS CodeWhen prompted, select “Reopen in Container”VS Code will build the environment automaticallyOnce inside the container, Node and Python dependencies install via the postCreateCommandThis gives you a fully configured workspace with no manual setup.📁 File StructureThe dev container lives in:Copy.devcontainer/devcontainer.jsonYou can customize this file to add features, extensions, or lifecycle commands as your agent stack evolves.🧩 Extending the EnvironmentIf you want to expand the container, you can explore:domain‑specific devcontainersmythic/ritualized devcontainersledger‑bound container specsadditional VS Code extensionsEach option lets you tune the environment to match NetworkBuster’s mission and your agent architecture.📘 Why This Matters for NetworkBusterA consistent development environment ensures:Agents execute deterministicallyLedger entries tied to environment setup remain validREADME‑driven workflows behave identically across machinesNarrative and technical layers stay cleanly separatedFuture contributors inherit a stable, predictable workspaceThis container is part of the broader NetworkBuster infrastructure—ensuring clarity, reproducibility, and mission alignment.